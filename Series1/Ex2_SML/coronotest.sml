val b = [1,2,3];

val a = Array.fromList(b);
Array.update(a,2,32);
val my = Vector.fromList([1,2,3,4]);
val my = Array.fromList([]);
val myfirst = Array.array (3,[1,2]);
Array.update(myfirst,2,[2]);
Array.update(myfirst,0,[0]);
Array.sub(myfirst,1);
Array.toVector(myfirst)
(*
Array.
*)